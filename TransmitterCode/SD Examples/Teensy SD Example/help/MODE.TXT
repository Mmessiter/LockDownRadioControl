This lets you select whether your transmitter is Mode 1 or Mode 2.

The default is Mode 1.

If the selected mode is wrong, the elevator and throttle appear to 
be swapped over.

After changing mode, you may also need to move the tiny spring 
inside the transmitter.